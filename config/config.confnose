# Folder to run server from
serverFolder = "server"

# Command to run server
toRun = "java -Xmx1G -jar server.jar --nogui"

# Discord Webhook Uri
webhookUri = ""

# If sending webhooks should be enabled (True / False)
webhooks = False